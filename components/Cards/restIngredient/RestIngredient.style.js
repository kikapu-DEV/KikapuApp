import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    detailContainer2:{
        padding: 20,
        alignItems: 'center'
    },
    dSubContainer:{
        flexDirection: "row",
        gap: 10,
        padding: 10,
        width: 150

    }
})

export default styles;